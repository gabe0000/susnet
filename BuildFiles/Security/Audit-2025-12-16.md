# Security & Hygiene Audit – 2025-12-16

Scope: high-level review (no credentials changed yet) to flag where to harden and what to back up.

Backups taken
- `/home/gabe0000/backups/asterisk-2025-12-16.tar.gz` containing `/etc/asterisk`, `/var/lib/asterisk/rpt_extnodes*`, `/opt/susnet-api/susnet_api.py`.

Findings (fix next)
- Secrets in configs: Asterisk `manager.conf` (admin/allmon3/susnet-monitor), IAX registers (`iax.conf`, `rpt_http_registrations.conf`), API envs, APRS env, cron URL for GMRS extnodes. These should be moved to protected env files and rotated from defaults.
- Service exposure: `susnet-api` listens on 0.0.0.0; AMI bound to 127.0.0.1 (good). Consider fronting API with a proxy/auth and binding uvicorn to localhost.
- File permissions: ensure env/secret files are root/asterisk readable only; verify `/var/lib/asterisk/rpt_extnodes_gmrs` remains 0644 asterisk-owned after cron runs.
- Systemd hardening: `susnet-api.service` currently runs as root; add NoNewPrivileges/ProtectSystem/ProtectHome and consider running under non-root user if possible.
- Cron fetch: GMRS extnodes pulled over HTTP; prefer HTTPS/checksum if source supports it. Command reloads app_rpt each time—fine but noisy; monitor for failures.
- Logs: review for tokens/callsigns before publishing. Ensure debug levels are minimal in production.

Current extnodes split (working)
- Global extnode file: `/var/lib/asterisk/rpt_extnodes` (ASL/stock) set in `[general]`.
- 531121 stanza override: `extnodefile=/var/lib/asterisk/rpt_extnodes_gmrs`.
- Cron job (root, every 15 min) refreshes GMRS list from `http://66.135.20.206/nodes/nodes.pl`, writes to `/var/lib/asterisk/rpt_extnodes_gmrs`, fixes ownership/perms, then `asterisk -rx 'module reload app_rpt.so'`.

Next actions
- Move credentials to env files, rotate manager/IAX/HTTP secrets, and lock manager users to least privilege.
- Bind API to localhost + add auth (or firewall) for remote access.
- Apply systemd hardening to `susnet-api.service`.
- Add a periodic permission check (or one-time verify) for `/etc/asterisk` and extnode files.
- Replace GMRS fetch with HTTPS/checksum if available; otherwise log/alert on fetch failures.
