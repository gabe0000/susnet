# Agents Folder

Use this folder for coding-agent playbooks and operational checklists. The GitHub remote at https://github.com/gabe0000/susnet is **documentation only**—never push code, logs, binaries, venvs, or secrets there.

Key files:
- `UpdatePlaybook.md` — doc-only publish/runbook.
- `AgentOps.md` — quick-start and safeguards for agents.

If you need to restart services after changes, use the local helper `/home/gabe0000/restart_susnet.sh` (do not publish it).
