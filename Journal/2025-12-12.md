# 2025-12-12 (Support + TTS sweep)

**Recap (detailed)**
- Closed the loop on support/UX tickets across APRS, Meshtastic, TTS, and AllStar favorites. Added long-form notes to help future readers reproduce fixes.
- Unified backend/frontends (public + private) and deployed to the live site and services.
- Added deployment notes so agents know where to copy/restart.

**What changed (teaching notes)**
- APRS readability (TKT-1018): API now emits parsed entries (timestamp/from/to/message) ordered newest-first; UI renders parsed entries and filters client-side. Lesson: keep API order explicit (`order=newest_first`) and avoid double-reversing in the UI.
- Meshtastic TTS context (TKT-1019): Listener now announces channel name/number and friendly long names for to/from, for both DM and broadcast; obeys mute/solo + rate-limit. Lesson: always include path context (channel + destination) in audio to reduce operator ambiguity.
- Ticket TTS confirmation (TKT-1020): Ticket creation triggers a TTS with the ticket ID and returns the ID to the UI; modal submission blurs inputs to reduce mobile cursor glitches. Lesson: surface IDs in both audio and UI for accessibility and confirmation.
- TTS send module (TKT-1021/1022 duplicate): Added a dedicated TTS panel (text + optional node override) wired to `/api/tts`; marked 1022 as duplicate. Lesson: consolidate duplicate asks into one module and update both tickets.
- AllStar favorites safety (TKT-1023): Favorites are tap-to-fill, long-press-to-connect; added a “disconnect first” toggle (default on). Lesson: long-press for irreversible actions + explicit toggle for destructive steps.
- Meshtastic send channel (TKT-1024): UI now sends the selected channel; backend queues sends with dest+channel via the listener queue. Lesson: carry channel explicitly through the send path to avoid defaulting to channel 0.
- Meshtastic logging: Listener logs include channel name/number in text/JSON entries so troubleshooting shows full routing context.
- Deployment helper: Added `DEPLOY.md` with paths/commands for UI/API/listener and restart steps.

**Action items / tests**
- Validate APRS parsing against live feed; adjust parser if edge cases appear.
- Send/receive Meshtastic DM and channel messages to confirm TTS phrasing with long names and channel labeling.
- Submit a test support ticket and verify spoken ID.
- Exercise TTS send panel and AllStar favorites long-press on touch; verify “disconnect first” toggle.
