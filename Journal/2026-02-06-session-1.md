# 2026-02-06 Session 1

## Summary
- Implemented SusNet V2 API topology: module APIs + core gateway API.
- Retargeted Node-RED flows to V2 gateway endpoints.
- Added core stack compose file and documented API contracts.
- Started module API containers and core gateway; validated health.

## Changes
- New services:
  - `services/core-api`
  - `services/module-allstar`
  - `services/module-gmrshub`
  - `services/module-aprs`
  - `services/module-meshtastic`
- New stack: `ops/stacks/susnet-core.compose.yml`
- New doc: `docs/API_CONTRACTS.md`
- Updated Node-RED flow pack to call gateway (`susnet-core-api:8080`).
- Node-RED now V2-only (no v1 host endpoints).

## Runtime notes
- Core gateway listening on `http://susnet.local:8090`.
- Node-RED dashboard uses gateway endpoints.
- Module APIs proxy v1 endpoints for now.

## Validation
- `GET /api/health` shows all modules OK.
- `GET /api/allstar/nodes` returns redacted nodes.
- APRS/Meshtastic routes return expected envelopes.
