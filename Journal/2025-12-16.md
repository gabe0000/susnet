# 2025-12-16 SusNet Ops Journal

## Summary
- Continued Meshtastic UI/API work: channel names, message log persistence, APRS UI path pill, APRS TTS behaviors.
- APRS module renamed to aprs-listener (separate from Meshtastic); widened filter and added TTS modes.
- Addressed APRS TTS chatter and watch=ALL gating; default mode now "me" to limit announcements.
- Meshtastic log now merges text history so messages don’t vanish on refresh/restart.
- Restarted all services to apply changes.

## Details
- APRS:
  - Listener now supports TTS modes (off/me/all) with per-character callsign spelling; beacons and non-target traffic summarized briefly.
  - Watch="ALL" truly watch-any; distance filter still applied. TTS gated so only target packets announce unless mode="all".
  - Env renamed to `/etc/default/aprs-listener`; service `aprs-listener.service` running (replaces meshtastic-aprs.service).
  - UI APRS panel includes TTS dropdown (Off/Me only/All) and still shows path pill popup.
- Meshtastic:
  - Message API merges `messages.txt` fallback every fetch so history persists.
  - Channel names/DM labels corrected; message input is multiline.
- Services restarted via `restart_susnet.sh` after config updates.
- AllStar:
  - Rebuilt `/etc/asterisk/rpt.conf` after corruption, parking node 1967 so 531121 owns its USB.
  - SimpleUSB now only targets 66190/531121; Asterisk restarted cleanly (app_rpt active).
  - API connect calls use ilink 13 (permanent) and Status buttons now send on-air *70 via `/api/status-tone`.
  - Added AMI listener + SSE endpoint (`/api/events/ami`) and UI stream; TX/RX/link events push in real time from localhost AMI (`susnet-monitor` user).
- Security audit started:
  - Snapshot backup: `/home/gabe0000/backups/asterisk-2025-12-16.tar.gz` (asterisk configs, extnodes, susnet_api.py).
  - Documented GMRS extnodes split and cron fetch; noted next steps to move secrets to env files, harden manager/API binds, and add systemd hardening.
- Hardening applied:
  - susnet-api systemd hardened (NoNewPrivileges, ProtectSystem, ProtectHome, PrivateTmp, restricted address families) and bound to 127.0.0.1.
  - Tightened perms on manager/iax/http reg/APRS env (0640).
  - Restarted meshtastic-listener and aprs-listener to recover from broken pipe/TTS gap.

## Next steps
- Add Meshtastic master TTS toggle (parity with APRS) if desired.
- Verify APRS UI shows saved TTS mode after hard refresh; adjust cache-busting if needed.
- Confirm APRS sends to W4VDX-10/-6 appear and announce with current filter (watch=ALL, radius=200mi).
